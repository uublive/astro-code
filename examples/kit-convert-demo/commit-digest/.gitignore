# Runtime output — never committed
_report/
raw/
audit_output/

# Parity harness's per-run produced output — regenerated by
# tools/parity_check.py on every invocation, never the committed fixture.
tools/parity/fixtures/*/actual_output.json

# Python
__pycache__/
*.pyc

# OS noise
.DS_Store

# dist/ IS committed (git add -f dist/kit.zip) — a global gitignore may
# exclude dist/, hence the -f.
