{
  "version": 1,
  "max_concurrent_agents": 6,
  "use_worktrees": true,
  "registry_branch": "astro-registry",
  "registry_remote": "origin",
  "models": {
    "_comment": "Per-role model tier: opus | sonnet | haiku. Remove a role to inherit the session model.",
    "planner": "opus",
    "researcher": "sonnet",
    "executor": "sonnet",
    "verifier": "opus",
    "discover": "haiku"
  }
}
