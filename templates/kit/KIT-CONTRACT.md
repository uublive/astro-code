# Astro Kit Contract (manifest v4)

Condensed authoring contract for Astro kits, distilled from the AstroKit registry's
`CLAUDE.md`. Every agent working on this kit must respect it — it is what makes the
kit installable and runnable by Astro. The vendored validator enforces the machine-
checkable parts: `python3 tools/validate_manifest.py kit.json`.

## Kit anatomy

```
<kit-root>/
├── kit.json              # manifest v4 (this contract)
├── registry-entry.json   # ready-to-paste kits.json v4 entry for a future registry
├── src/                  # EVERYTHING the kit ships — zipped root-relative
│   ├── CLAUDE.md         # REQUIRED — kit run instructions for Astro
│   ├── README.md         # REQUIRED — what the kit does, how to use it
│   ├── EXAMPLES.md       # REQUIRED — see format below
│   ├── recipes/<id>.yaml # the kit's recipe: phased execution contract
│   ├── scripts/          # report/artifact generators (python3)
│   ├── reference/        # static reference data (JSON)
│   ├── schemas/          # output JSON schemas
│   └── generate_report.py
├── tools/                # build + validation (NOT shipped in the zip)
└── dist/kit.zip          # built artifact — commit with `git add -f`
```

## kit.json — manifest v4

Required fields: `name` (must equal the kit id / directory name), `version` (semver),
`description`, `author`, `contents` (auto-generated by the build), `download_url`,
`sha256` (auto-generated), `manifest_version: 4`, `contract_version: "^1.0.0"`.

### requires.tools[] — four source types, EXACT pins only

- `{ "source": "apt",  "name": "<debian-pkg>", "version": "<exact>" }` — omitting
  `version` is a validator warning (supply-chain risk).
- `{ "source": "pip",  "name": "<pep503-name>", "version": "1.45.0", "verify": "..." }`
  — name lowercase/hyphens; ranges (`>=`, `^`, `*`, `latest`) are REJECTED.
- `{ "source": "npm",  "name": "@scope/pkg", "version": "2.1.0", "global": true }`
  — exact semver only.
- `{ "source": "url",  "url": "https://...", "sha256": "<64-hex>", "filename": "...",
  "executable": true }` — https only; sha256 is 64-char lowercase hex.

**Base-image allowlist — DO NOT declare these** (Astro's image already has them):
`bash`, `coreutils`, `grep`, `sed`, `awk`, `jq`, `git`, `curl`, `wget`, `tar`, `unzip`.
Declare only tools OUTSIDE this list (semgrep, gitleaks, weasyprint, …).

### outputs.artifacts[] — the email_attachment tag

Artifacts are objects `{ "path": "<relative path>", "tags": ["email_attachment"] }`.
**At most ONE artifact** may carry `"email_attachment"` — it marks the single file
Astro attaches when replying by email. Zero tagged artifacts = text-only reply.
Many equally-primary files? Bundle them into one zip and tag the zip.

## The recipe (`src/recipes/<id>.yaml`)

The recipe is the kit's execution contract: `name`, `description`, `version`, and a
`phases:` list. Each phase declares:

- `name` — snake_case identifier
- `goal` — imperative instructions for the phase (what to do, step by step)
- `constraints` — hard rules the phase must obey (quoted strings)
- `input` / `output` — the files the phase reads/writes (typically under `_report/`)

Conventions: runtime output goes under `_report/`; state travels through
`_report/state.json`; deliverables are produced by `scripts/*.py` — **NEVER
hand-written** ("if script fails, debug and fix — do NOT create the artifact
manually" belongs in the constraints of report phases).

## EXAMPLES.md — required format

Loaded by Astro to understand how to invoke the kit. Required sections:

1. `## Quick Start` — single simplest invocation, no options
2. `## Examples` — at least 2, each with **Prompt**, **Arguments**,
   **Expected workflow**, **Produces**
3. `## Argument Reference` — table: Argument | Type | Default | Description
4. `## Common Patterns` — tips, gotchas, multi-kit workflows

## Build & definition of done

A kit is DONE when all of these hold:

- `python3 tools/validate_manifest.py kit.json` exits 0
- `./tools/build_kit.sh` succeeds: builds `dist/kit.zip` (root-relative paths,
  `CLAUDE.md` at zip root), fills `sha256` + `contents[]` in `kit.json`, and
  refreshes `registry-entry.json`
- `src/EXAMPLES.md` has all 4 required sections
- the recipe exists, parses as YAML, and its phases' inputs/outputs are consistent
- every declared artifact is actually produced by the workflow the recipe describes
- `dist/kit.zip` is committed (`git add -f dist/kit.zip`)

## Publishing to a hosted Astro instance

`dist/kit.zip` ships only `src/` — the manifest lives beside it, for the GitHub
registry model. A hosted Astro instance instead wants ONE file: the kit zip **with
`kit.json` inside at the root** (the registry-schema manifest — `id`, `name`,
`description`, `version`, `tags`, …, which `registry-entry.json` already holds).
`tools/publish_kit.py` builds that upload package (every `src/` file root-relative
+ `kit.json` at the root) and POSTs it to `POST /api/kit-packages` (admin auth;
the server assigns `download_url` + `sha256`). Run it via `/astro-publish-kit`, or
directly: `python3 tools/publish_kit.py --kit-root . --base <url> --email <admin>`
(password via `$ASTRO_ADMIN_PASSWORD`). Re-uploading an existing id+version is
rejected — bump `version` or publish with `--replace`.
